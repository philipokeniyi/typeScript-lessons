let greetings: string = "Hello, TypeScript!";

console.log(greetings);

// number type
let userId: number = 6442.9;

// boolean type

let isLoggedIn: boolean = false;

// array type
let favoriteColors: string[] = ["red", "blue", "green"];

// tuple type
let user: [string, number] = ["Philip", 30];

// enum type
enum Direction {
  Up,
  Down,
  Left,
  Right,
}
 

export {};
